default_stages: [commit]
repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v3.1.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-json
    -   id: check-yaml
    -   id: debug-statements
-   repo: local
    hooks:
    -   id: sanity-check
        name: sanity-check
        entry: bash scripts/sanity_test.sh
        language: system
        verbose: true
        stages: [merge-commit, commit, push]
    -   id: sanity-check-file
        name: sanity-check-file
        entry: scripts/sanity_check.py
        language: python
        verbose: true
        pass_filenames: false
        args: [--mode=diff]
        stages: [merge-commit, commit, push]
